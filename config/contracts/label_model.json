{
  "schema_version": 1,
  "description": "Configurable first-level and second-level label model for sources, candidate pool items, reports and future domains.",
  "core_rule": "Labels are configured once and attached generically. The first-level labels are the clustering/recommendation directory; do not create a separate tool directory for them.",
  "tables": {
    "label_sets": [
      "workspace_code",
      "domain_code",
      "code",
      "name",
      "scope_type",
      "target_types",
      "enabled",
      "config_json"
    ],
    "labels": [
      "label_set_id",
      "parent_label_id",
      "label_level",
      "code",
      "name",
      "color",
      "description",
      "sort_order",
      "enabled",
      "metadata_json"
    ],
    "content_labels": [
      "label_id",
      "target_type",
      "target_id",
      "assigned_by_user_id",
      "assignment_source",
      "confidence",
      "metadata_json"
    ]
  },
  "first_phase_label_sets": {
    "ai_sql_categories": {
      "source": "config/taxonomy/news_categories.json",
      "purpose": "The 10 first-level labels required by the current company SQL export and first-phase clustering/recommendation directory."
    }
  },
  "target_types": [
    "data_source",
    "workspace_source_link",
    "raw_item",
    "news_item",
    "dedupe_group",
    "recommendation_item",
    "daily_report_item",
    "weekly_report_item",
    "requirement",
    "journal_entry"
  ],
  "assignment_sources": [
    "manual",
    "seed",
    "rule",
    "model",
    "import"
  ],
  "rules": [
    "Source management pages may choose label sets and default label rules.",
    "Source management pages may configure which first-level and second-level labels a source should feed into.",
    "Second-level labels are stored as labels with parent_label_id pointing to a first-level label.",
    "Clustering and recommendation rules should reference label_set_code, label code and optional parent/child label paths.",
    "Candidate pool pages should display labels attached to the dedupe winner and its group.",
    "Company SQL export must map adopted daily report items to config/taxonomy/news_categories.json until the internal platform supports broader taxonomies.",
    "Adding hardware or semiconductor labels should add a label set or domain pack, not new columns."
  ]
}
