{
  "schema_version": 1,
  "description": "Configurable first-level and second-level label model for model tagging, candidate pool items, reports and future domains.",
  "core_rule": "A workspace owns one unified label policy. The first-level labels constrain model-generated news structure and post-dedupe label finalization; per-source label configuration is not used because one source may contain multiple directions.",
  "tables": {
    "label_sets": [
      "workspace_code",
      "domain_code",
      "code",
      "name",
      "scope_type",
      "target_types",
      "enabled",
      "config_json"
    ],
    "labels": [
      "label_set_id",
      "parent_label_id",
      "label_level",
      "code",
      "name",
      "color",
      "description",
      "sort_order",
      "enabled",
      "metadata_json"
    ],
    "content_labels": [
      "label_id",
      "target_type",
      "target_id",
      "assigned_by_user_id",
      "assignment_source",
      "confidence",
      "metadata_json"
    ]
  },
  "first_phase_label_sets": {
    "ai_sql_categories": {
      "source": "config/taxonomy/news_categories.json",
      "purpose": "The 10 first-level labels required by the current company SQL export and first-phase clustering/recommendation directory."
    },
    "ai_tools_categories": {
      "workspace_code": "ai_tools",
      "primary_categories": ["工具新功能", "工具新案例", "工具新技术"],
      "secondary_labels_by_primary": {
        "工具新功能": ["cursor", "claude code", "opencode", "codex"],
        "工具新案例": ["cursor", "claude code", "opencode", "codex"],
        "工具新技术": ["cursor", "claude code", "opencode", "codex"]
      },
      "purpose": "The AI tools workspace label set. It is intentionally isolated from planning_intel's company SQL categories."
    }
  },
  "workspace_label_policy": {
    "storage": "workspaces.config_json.label_policy",
    "fields": [
      "label_set_code",
      "allowed_primary_categories",
      "secondary_labels_by_primary",
      "default_category",
      "fallback_category",
      "tagging_stages"
    ],
    "planning_intel_default_label_set_code": "ai_sql_categories",
    "ai_tools_default_label_set_code": "ai_tools_categories",
    "tagging_stages": [
      "news_generation",
      "post_dedupe_labeling"
    ],
    "source_label_config": "disabled"
  },
  "target_types": [
    "data_source",
    "workspace_source_link",
    "raw_item",
    "news_item",
    "dedupe_group",
    "recommendation_item",
    "daily_report_item",
    "weekly_report_item",
    "requirement",
    "journal_entry"
  ],
  "assignment_sources": [
    "manual",
    "seed",
    "rule",
    "model",
    "import"
  ],
  "rules": [
    "Source management pages must expose the workspace unified label policy before per-source controls.",
    "Model prompts for raw-to-news generation must use workspaces.config_json.label_policy.allowed_primary_categories as the legal category list.",
    "Model prompts may use workspaces.config_json.label_policy.secondary_labels_by_primary as optional second-level labels under the selected primary category.",
    "After dedupe, model/rule label finalization must use the same workspace label policy, not per-source labels.",
    "Source management pages must not configure labels per source in the first phase; one source can contain multiple strategic directions.",
    "Second-level labels are stored as labels with parent_label_id pointing to a first-level label.",
    "Clustering and recommendation rules should reference label_set_code, label code and optional parent/child label paths.",
    "Candidate pool pages should display labels attached to the dedupe winner and its group.",
    "Company SQL export must map adopted daily report items to config/taxonomy/news_categories.json until the internal platform supports broader taxonomies.",
    "Adding hardware or semiconductor labels should add a label set or domain pack, not new columns."
  ]
}
