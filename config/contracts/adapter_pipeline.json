{
  "schema_version": 1,
  "description": "Ingestion adapter and deduplication pipeline contract.",
  "pipeline_order": [
    "workspace_ingestion_run",
    "data_source_config",
    "adapter_fetch",
    "raw_item_persist",
    "content_extract",
    "normalize_to_news_item",
    "dedupe_grouping",
    "scoring_and_recommendation",
    "generate_structured_news",
    "daily_report_draft",
    "editor_review",
    "company_sql_export"
  ],
  "ingestion_run_rule": "A workspace ingestion run selects enabled workspace_source_links, executes the registered adapters, persists raw_items idempotently, and records run-level source success/failure plus raw created/updated counts. Schedulers and workers must call this same service layer instead of bypassing adapters.",
  "adapter_rule": "Each source type may have its own input structure, but every adapter must persist the full raw payload and emit canonical raw_items. Normalization enriches and maps fields; it must not discard the original payload.",
  "adapter_network_rule": "HTTP adapters that fetch RSS, paper RSS, archive pages or sitemaps must not inherit host proxy environment variables by default; local, intranet and container-internal sources must be fetched directly unless a source-specific proxy is explicitly configured.",
  "raw_item_identity_rule": "raw_items are idempotent by data_source_id + entry_key. entry_key must fit raw_items.entry_key length 255. If an upstream feed id or redirect URL is longer, the ingestion layer must deterministically shorten it with a stable hash suffix while preserving the original value inside raw_payload_json.",
  "source_type_to_adapter": {
    "wiseflow": "WiseflowReadInfoAdapter",
    "rss": "RssFeedAdapter",
    "paper_rss": "RssFeedAdapter plus PaperMetadataEnricher",
    "page_monitor": "PageListingAdapter",
    "page_manual": "ManualPageAdapter",
    "crawler": "CustomCrawlerAdapter",
    "paper_api": "PaperApiAdapter",
    "paper_page": "PaperPageAdapter",
    "manual": "ManualNewsAdapter",
    "internal": "InternalSourceAdapter"
  },
  "canonical_raw_item_fields": [
    "id",
    "data_source_id",
    "domain_code",
    "visibility_scope",
    "sync_policy",
    "source_type",
    "source_name",
    "entry_key",
    "source_url",
    "canonical_url",
    "source_title",
    "raw_summary",
    "raw_content",
    "published_at",
    "fetched_at",
    "raw_payload_json",
    "source_specific_json",
    "extract_status",
    "extract_error"
  ],
  "canonical_news_item_fields": [
    "id",
    "raw_item_id",
    "domain_code",
    "visibility_scope",
    "sync_policy",
    "source_url",
    "canonical_url",
    "source_title",
    "content",
    "created_at",
    "focus_id",
    "source_kind",
    "source_name",
    "dedupe_key",
    "duplicate_of",
    "active",
    "normalization_status"
  ],
  "dedupe_stage": {
    "runs_after": "normalize_to_news_item",
    "runs_before": "scoring_and_recommendation",
    "reason": "Different source structures must first be normalized before they can be compared fairly. Scoring should only run on deduped winners.",
    "workspace_scope_rule": "dedupe_groups are unique by workspace_code + dedupe_key. Shared raw_items may be normalized independently into each enabled workspace, and each workspace owns its own winner/loser state.",
    "dedupe_key_length_rule": "dedupe_key must fit news_items.dedupe_key and dedupe_groups.dedupe_key length 512. Long canonical URLs or title/date keys are deterministically shortened with a stable hash suffix; canonical_url and raw source_url remain stored as full Text fields for lineage.",
    "hard_dedupe_key_order": [
      "canonical_url",
      "normalized_title + published_date_bucket when canonical_url is missing"
    ],
    "canonical_url_rules": [
      "lowercase scheme and host",
      "remove URL fragment",
      "strip trailing slash from path",
      "remove tracking query params such as utm_*, spm, ref, ref_src, fbclid, gclid"
    ],
    "representative_selection_order": [
      "has source_url/canonical_url",
      "source_kind priority: wiseflow gets legacy bonus because it may contain richer crawled material",
      "preferred/trusted source bonus",
      "longer useful content",
      "newer created_at"
    ],
    "result_fields": {
      "winner": "active = true, duplicate_of = null",
      "loser": "active = false, duplicate_of = winner.id or winner.candidate_key",
      "all_items": "raw_items remain preserved; dedupe never deletes raw records"
    }
  },
  "recommendation_stage": {
    "runs_after": "dedupe_grouping",
    "runs_before": "generate_structured_news",
    "current_api": "POST /api/pipeline/daily-runs for full pipeline; POST /api/recommendation/runs for recommendation-only reruns",
    "candidate_rule": "Only active dedupe winners from the requested workspace and target day_key can enter a recommendation run. Losers remain traceable but are not recommended directly.",
    "day_key_rule": "Daily recommendation filters candidates by (news_items.published_at or created_at) in Asia/Shanghai. Freshness scoring for backfill uses the target day end instead of the wall-clock current day.",
    "planning_intel_priority_rule": "The default planning_intel scorer is technical-intelligence-first and uses a content admission layer before daily selection. It boosts AI software, AI infrastructure, model engineering, inference/training, RAG, multi-agent, agent memory, hardware vendor technology, competitor technical roadmaps, AI chips, GPU clusters, data center architecture and telecom system signals. Source-level tags are only weak priors: a vendor or hardware source is not enough by itself; the item must contain concrete technical, architecture, AI, chip, data-center or telecom evidence. It demotes finance, funding, earnings, procurement/bidding, consumer gadgets, event promotion, marketing launch events, generic partnership, pure marketing, legal/meta discussion, clickbait, social/education off-scope items, aerospace/rocket off-scope items and off-scope academic/biomedical papers unless editors explicitly override.",
    "content_admission_levels": {
      "P0": "major insight, may affect technical roadmap, architecture, cost, standards or competitive landscape",
      "P1": "high-value technical signal, eligible for daily report",
      "P2": "medium-value observation, may enter daily when budget remains or weekly/observation pool",
      "P3": "low-value archive/search-only signal, not selected into daily by default",
      "R": "rejected or noise, not selected into daily by default"
    },
    "content_admission_dimensions": [
      "topic relevance",
      "expert impact",
      "evidence strength",
      "novelty",
      "technical maturity or implementation detail",
      "freshness",
      "noise penalty"
    ],
    "content_scorer_v2": {
      "config_path": "config/scoring/content_scorer_v2.json",
      "source": "Tech Insight Loop content_scorer_config.v3-enhanced-no-new-boards.json",
      "rule": "The recommendation service remains responsible for candidate selection and daily generation, but content admission must be persisted as structured fields on recommendation_items. Missing config must fall back to the existing baseline content-admission logic.",
      "persisted_fields": [
        "admission_level",
        "admission_score",
        "admission_pool",
        "noise_types_json",
        "reject_reasons_json",
        "scorer_breakdown_json",
        "expert_routes_json"
      ],
      "source_priors": [
        "data_sources.source_score",
        "metadata.source_tier",
        "metadata.source_channel_type",
        "metadata.board_relevance_json",
        "metadata.source_score_breakdown_json",
        "metadata.expert_routes"
      ]
    },
    "selection_caps": {
      "source_daily_limit": "hard per-source selected item cap",
      "paper_rss_daily_ratio": "paper_rss selected items are capped at about 10% of the requested limit",
      "pool_daily_ratio": "one admission pool should not exceed about 40% of the requested limit",
      "selected_levels": "select P0/P1 first, then clean P2 with daily-worthy technical signal; noisy P2, P2 paper_rss, P3 and R are not selected by default"
    },
    "default_limit": 15,
    "default_source_daily_limit": 2,
    "score_dimensions": [
      "quality_score",
      "topic_score",
      "freshness_score",
      "feedback_score",
      "diversity_score",
      "source_score",
      "heat_score",
      "final_score"
    ],
    "writes": [
      "recommendation_runs",
      "recommendation_items",
      "generated_news for selected recommendation_items",
      "daily_reports and daily_report_items when create_daily_draft is true"
    ],
    "lineage_rule": "generated_news.content_json.source must preserve news_item_id, raw_item_id and data_source_id so adopted daily report items can be traced back to raw_items.raw_payload_json.",
    "adoption_rule": "Recommendation creates draft daily_report_items with adoption_status = 2 for selected items. Editors may later change adoption_status on daily_report_items; news_items and raw_items are never overwritten.",
    "workspace_scope_rule": "Daily reports are unique by workspace_code + domain_code + day_key so multiple workspaces can generate same-day reports independently."
  },
  "weekly_report_stage": {
    "status": "phase_1_backend",
    "implemented_endpoints": [
      "GET /api/weekly-reports",
      "POST /api/weekly-reports",
      "GET /api/weekly-reports/{id}",
      "POST /api/weekly-reports/{id}/publish",
      "PATCH /api/weekly-report-items/{id}"
    ],
    "source_rule": "Weekly draft generation starts from daily_report_items with adoption_status=2 in the requested ISO week, published daily reports by default, and keeps daily_report_item_id plus generated_news_id for lineage.",
    "adoption_rule": "Generated weekly_report_items start with adoption_status=1 as weekly candidates. Admins set adoption_status=2 for final weekly adoption or 0 to remove from the weekly report.",
    "workspace_scope_rule": "Weekly reports are unique by workspace_code + domain_code + week_key so multiple workspaces can build the same ISO week independently."
  },
  "generator_stage": {
    "current_provider": "MiniMax China OpenAI-compatible chat/completions at MINIMAX_BASE_URL or default https://api.minimaxi.com/v1 when MINIMAX_GENERATION_ENABLED=true and MINIMAX_API_KEY is set; deterministic rule_v1 fallback otherwise",
    "default_generation_timeout_seconds": 45,
    "provider_rule": "Model output must be strict JSON, must be Simplified Chinese except for proper nouns, and category must be one of the workspace label policy primary labels. Source trace IDs are forced by backend after generation.",
    "news_format_rule": "For planning_intel/company_sql_v1, generated_news.content_json must contain background, effects, eventSummary, technologyAndInnovation and valueAndImpact so later SQL export can map without guessing.",
    "fallback_rule": "LLM timeout or failure must not abort the daily pipeline; selected recommendations still generate rule_v1:fallback drafts with the same required content_json fields, but generation_status must be fallback_needs_review and standard Company SQL export must reject them until an LLM-ready or edited-ready version exists.",
    "live_acceptance_command": "MINIMAX_GENERATION_ENABLED=true MINIMAX_API_KEY=<secret> python3 scripts/validate_minimax_generation_acceptance.py",
    "latest_live_acceptance": {
      "output_json": "outputs/minimax/minimax_generation_acceptance.json",
      "response_text": "outputs/minimax/minimax_live_acceptance_20260703.response.txt",
      "status": "passed with MiniMax-M2.7-highspeed; unsupported numeric claims not present in source are rejected by the validator"
    },
    "rerun_api": "POST /api/daily-reports/{report_id}/regenerate-generated-news can rerun non-ready or selected generated_news rows for a draft daily report."
  },
  "scheduled_daily_pipeline": {
    "current_job": "app.pipeline.daily.run_daily_pipeline_job",
    "current_api": "POST /api/pipeline/daily-runs",
    "scheduler_default_mode": "daily_pipeline",
    "default_source_types": ["rss", "paper_rss", "page_manual", "page_monitor", "wiseflow"],
    "legacy_ingestion_only_mode": "ingestion_only",
    "enabled_by": "INGESTION_SCHEDULER_ENABLED=true",
    "uses_scope_settings": [
      "INGESTION_SCHEDULER_WORKSPACE_CODE",
      "INGESTION_SCHEDULER_SOURCE_TYPES",
      "INGESTION_SCHEDULER_LIMIT",
      "INGESTION_CONCURRENCY",
      "INGESTION_SOURCE_TIMEOUT_SECONDS",
      "INGESTION_SCHEDULER_DAILY_TIME",
      "INGESTION_SCHEDULER_TIMEZONE",
      "DAILY_PIPELINE_DAY_OFFSET_DAYS"
    ],
    "schedule_rule": "When INGESTION_SCHEDULER_DAILY_TIME is set, the scheduler must wait for that HH:MM or HH:MM:SS wall-clock time in INGESTION_SCHEDULER_TIMEZONE instead of running immediately on container start. If DAILY_PIPELINE_DAY_OFFSET_DAYS is non-zero, the scheduler must pass the derived day_key to the daily pipeline; for a morning planning_intel job use -1 so a 09:00 Asia/Shanghai run generates yesterday's daily report.",
    "ordered_steps": [
      "optional workspace_ingestion_run",
      "normalize_to_news_item",
      "dedupe_grouping",
      "scoring_and_recommendation",
      "generate_structured_news",
      "daily_report_draft"
    ],
    "rule": "The scheduled job must call the same service functions as the manual APIs. It must not bypass adapter, normalization, dedupe or recommendation contracts."
  },
  "historical_backfill_stage": {
    "status": "phase_2_modes_v1",
    "purpose": "Recover target-date raw/news candidates that cannot be restored from current RSS windows.",
    "run_record": "Reuse ingestion_runs with run_type=historical_backfill and explicit params_json.target_day_start, target_day_end, backfill_mode, source_scope, retry_policy, include_undated and manual_items count.",
    "endpoint": "POST /api/ingestion/backfill-runs",
    "current_behavior": "Fetch enabled workspace sources through the selected backfill mode, persist only raw inputs whose published_at falls inside the target day range by default, and record fetched/in-range/out-of-range/missing-date counts in summary_json.",
    "implemented_modes": [
      "rss_window",
      "paper_api",
      "archive_page",
      "sitemap",
      "manual_import"
    ],
    "mode_caveat": "paper_api depends on a registered source adapter. rss_window is not a full archive guarantee. archive_page and sitemap are lightweight URL discovery modes, not a universal crawler.",
    "mode_priority": [
      "rss_window: low-cost current-window recovery for target dates still visible in feeds; not a complete historical guarantee",
      "paper_api: arXiv/OpenAlex/Semantic Scholar/OpenReview/Hugging Face Papers date queries",
      "archive_page: vendor blogs, standard organizations, cloud/hardware archives and paginated lists",
      "sitemap: discover dated article URLs from official sitemaps",
      "retry_failed: low-concurrency long-timeout retry for failed sources",
      "manual_import: CSV/SQL/manual raw import with full raw_payload_json lineage"
    ],
    "success_metrics": [
      "target-date raw_items created or updated",
      "target-date news_items normalized",
      "active winners created or replaced after dedupe",
      "recommendation candidates and selected daily report items added"
    ],
    "rule": "Backfill output must enter raw_items first and then reuse the same normalization, dedupe, recommendation and SQL export pipeline. It must not insert generated_news or SQL rows directly."
  },
  "soft_clustering_stage": {
    "status": "phase_2",
    "purpose": "Group same-topic items across different URLs without automatically deleting them.",
    "signals": [
      "title similarity",
      "summary/content embedding similarity",
      "same company/product/model names",
      "same publication date window"
    ],
    "rule": "Soft clusters support editor review and weekly aggregation; they do not replace hard dedupe in phase 1."
  }
}
