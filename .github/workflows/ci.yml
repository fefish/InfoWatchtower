name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install backend
        working-directory: backend
        run: pip install ".[dev]"
      - name: Check production deployment config
        run: python scripts/check_prod_deploy.py --env-file deploy/env.production.example
      - name: Test backend
        working-directory: backend
        env:
          DATABASE_URL: ""
        run: |
          coverage run -m pytest
          coverage report
          coverage xml -o coverage.xml
          coverage html -d htmlcov
      - name: Upload backend coverage report
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage-report
          path: |
            backend/coverage.xml
            backend/htmlcov

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install frontend
        working-directory: frontend
        run: npm install
      - name: Test frontend
        working-directory: frontend
        run: npx vitest run
      - name: Build frontend
        working-directory: frontend
        run: npm run build
      # Playwright e2e（frontend/e2e/*.e2e.ts）不进 CI 门禁：chromium 每次需
      # ~100MB 浏览器下载，显著拖慢并偶发抖动。本地或发版前用 `make e2e` 手动跑。
